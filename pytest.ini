[pytest]
testpaths = tests
markers =
    integration: loads real embedding models and downloads weights on first run
filterwarnings =
    ignore::UserWarning
    ignore::DeprecationWarning
asyncio_mode = auto
