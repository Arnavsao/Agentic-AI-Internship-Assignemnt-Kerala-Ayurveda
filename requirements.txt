# ── Streamlit UI dependencies ──
#
# The UI is a thin HTTP client over the FastAPI backend, so it needs no ML
# stack of its own. It previously pulled in torch, sentence-transformers,
# chromadb, and the whole LangChain suite (~2.5 GB) because it ran retrieval
# and the agents in-process; all of that now lives behind the API.
#
# Backend dependencies: backend/requirements.txt

streamlit>=1.28.0
requests>=2.31.0
python-dotenv>=1.0.0
